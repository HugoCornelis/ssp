#!/usr/bin/perl -w

my $config
    = {
       files => [
		 './configure.ac',
		 './test-dist',
		 './tests.config',
		 './tests/tests.config',
		],
       labels => {
		  email => '',
		  label => '',
		  major => '',
		  micro => '',
		  minor => '',
		  monotone_id => `mtn automate get_current_revision_id`,
		  package => '',
		 },
      };

return $config;

